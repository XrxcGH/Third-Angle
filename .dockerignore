# The data directory is restored from R2 at boot; baking a database into the
# image would ship a stale copy and, worse, a copy of the private one.
data/
node_modules/
.git/
.github/
tests/
*.log
.env
.env.*
# The published preview is a build artefact of the site, not part of it.
data/preview.html
